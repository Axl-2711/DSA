// int max = merge[0];
        // for (int i = 0; i < merge.length; i++) {
        //     if (max < merge[i]) {
        //         max = merge[i];
        //     }
        // }
        // System.out.println(max);